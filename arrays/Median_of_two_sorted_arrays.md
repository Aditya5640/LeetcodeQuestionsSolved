Use two pointers i and j to traverse both sorted arrays.
Compare the current elements and insert the smaller one into the result array.
Move the corresponding pointer forward. Continue until one array is exhausted.
Finally, copy all remaining elements from the non-exhausted array into the result. 
Since both arrays are already sorted, the resulting array remains sorted.
